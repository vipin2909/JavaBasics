package derekTutorial;

public interface Drivable {
    // basically interface is an empty class that provides some one or tells someone which methods they
    // must implement if they want to use this interface

    double PI = 3.14;
    int getWheel();
    void setWheels(int numWheels);
    double getSpeed();
    void setSpeed(double speed);

}
